ALGO-LAB
# Sorting Algorithms

## 👤 Student Info
- Name: Raihan  
- ID: C243037  

## 📌 Algorithms
- Insertion Sort → Simple but slow (O(n²))  
- Merge Sort → Efficient, uses extra memory (O(n log n))  
- Quick Sort → Fastest in most cases (O(n log n))  

## 📊 Observation
- Quick Sort performed the best  
- Merge Sort was slightly slower due to extra operations  
- Insertion Sort was very slow for large inputs  
- For small data, Insertion Sort can be fast  
- Very small time shows 0.000 ms (too fast to measure)  
